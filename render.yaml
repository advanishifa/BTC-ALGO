
services:
  - type: web
    name: tradingview-webhook
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn btc_trading_bot:app --bind 0.0.0.0:$PORT
